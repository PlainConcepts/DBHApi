{
  "name": "DBHApi",
  "subdomain": "dbhapi",
  "version": "0.0.1",
  "scripts": {
    "start": "node ./bin/www",
    "test": "mocha --require test/support/env --reporter spec --bail --check-leaks test/specs",
    "test-ci": "istanbul cover node_modules/mocha/bin/_mocha --report lcovonly -- --require test/support/env --reporter spec --check-leaks test/specs"
  },
  "dependencies": {
    "async": "^0.9.0",
    "azure-storage": "^0.3.3",
    "body-parser": "~1.8.1",
    "debug": "~2.0.0",
    "express": "~4.9.0",
    "morgan": "~1.3.0",
    "nconf": "^0.6.9",
    "node-uuid": "^1.4.1"
  },
  "devDependencies": {
    "after": "0.8.1",
    "istanbul": "0.3.2",
    "mocha": "~1.21.4",
    "should": "~4.0.4",
    "supertest": "~0.13.0"
  },
   "engines" : { "node" : ">=0.10.0" }
}
